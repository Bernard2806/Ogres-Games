---
// Servers.astro
import serversData from '../data/servers.json';
---

<section id="servers" class="servers-section">
  <div class="container">
    <h2 class="section-title">SERVIDORES OFICIALES</h2>
    <p class="section-desc">Únete a nuestra comunidad gamer. Servidores dedicados con soporte 24/7 y contenido personalizado.</p>
    
    <div class="servers-dashboard">
      
      <!-- Left column: Servers list -->
      <div class="servers-list">
        {serversData.map((server) => (
          <div class="server-node pixel-border" data-server-ip={server.ip}>
            <div class="server-node-header">
              <span class="game-tag tag tag-cyan">{server.game}</span>
              <div class="status-indicator">
                <span class={`status-led ${server.status.toLowerCase() === 'online' || server.status.toLowerCase() === 'active' ? 'led-online' : 'led-offline'}`}></span>
                <span class="status-text terminal-font">{server.status}</span>
              </div>
            </div>
            
            <h3 class="server-name arcade-font">{server.name}</h3>
            <p class="server-desc">{server.description}</p>
            
            <div class="server-meta-grid terminal-font">
              <div class="meta-item">
                <span class="label">VERSION:</span>
                <span class="value">{server.version}</span>
              </div>
              <div class="meta-item">
                <span class="label">PLAYERS:</span>
                <span class="value">{server.players.online} / {server.players.max}</span>
              </div>
              <div class="meta-item-full">
                <span class="label">DIRECCIÓN IP:</span>
                <span class="value highlight-ip clickable-ip" data-ip={server.ip} title="Click para copiar IP">
                  {server.ip}
                  <span class="copy-hint arcade-font">[COPIAR]</span>
                </span>
              </div>
            </div>
            
            <div class="server-key-features">
              <h4 class="features-title arcade-font">CARACTERÍSTICAS:</h4>
              <ul class="features-list">
                {server.features.map(feat => (
                  <li class="feature-item">⭐ {feat}</li>
                ))}
              </ul>
            </div>
            
            <div class="server-actions">
              <button class="pixel-button pixel-button-green copy-ip-btn" data-ip={server.ip}>
                📋 COPIAR IP
              </button>
              <a href={server.discordUrl} target="_blank" rel="noopener noreferrer" class="pixel-button pixel-button-purple">
                💬 UNIRSE AL DISCORD
              </a>
            </div>
          </div>
        ))}
      </div>
      
      <div class="clipboard-toast arcade-font" id="clipboard-toast">
        ¡IP COPIADA AL PORTAPAPELES!
      </div>
      
    </div>
  </div>
</section>

<script>
  // Clipboard copying mechanism
  const copyButtons = document.querySelectorAll('.copy-ip-btn');
  const clickableIps = document.querySelectorAll('.clickable-ip');
  const toast = document.getElementById('clipboard-toast');

  function copyIpToClipboard(ipText: string) {
    navigator.clipboard.writeText(ipText).then(() => {
      if (toast) {
        toast.classList.add('show');
        setTimeout(() => {
          toast.classList.remove('show');
        }, 2500);
      }
    }).catch(err => {
      console.error('Failed to copy text: ', err);
    });
  }

  copyButtons.forEach(btn => {
    btn.addEventListener('click', () => {
      const ip = btn.getAttribute('data-ip');
      if (ip) copyIpToClipboard(ip);
    });
  });

  clickableIps.forEach(ipSpan => {
    ipSpan.addEventListener('click', () => {
      const ip = ipSpan.getAttribute('data-ip');
      if (ip) copyIpToClipboard(ip);
    });
  });
</script>

<style>
  .servers-section {
    padding: 100px 0;
    border-top: 3px solid #141026;
    background-color: #07050e;
  }

  .servers-dashboard {
    display: grid;
    grid-template-columns: 1fr;
    gap: 40px;
    margin-top: 40px;
    position: relative;
  }

  .servers-list {
    display: flex;
    flex-direction: column;
    gap: 30px;
  }

  .server-node {
    background-color: var(--color-panel-bg);
    border-color: #231c3c;
    padding: 30px;
    border-radius: 4px;
    transition: all 0.3s ease;
  }

  .server-node:hover {
    border-color: var(--neon-cyan);
    box-shadow: 0 0 15px var(--neon-cyan-glow);
  }

  .server-node-header {
    display: flex;
    justify-content: space-between;
    align-items: center;
    margin-bottom: 20px;
  }

  .status-indicator {
    display: flex;
    align-items: center;
    gap: 8px;
  }

  .status-led {
    width: 10px;
    height: 10px;
    border-radius: 50%;
  }

  .led-online {
    background-color: var(--neon-green);
    box-shadow: 0 0 10px var(--neon-green);
    animation: led-blink 1.5s infinite alternate;
  }

  .led-offline {
    background-color: #ff0055;
    box-shadow: 0 0 10px #ff0055;
  }

  @keyframes led-blink {
    0% { opacity: 0.6; }
    100% { opacity: 1; }
  }

  .status-text {
    font-size: 1.1rem;
    color: #fff;
  }

  .server-name {
    font-size: 1.2rem;
    margin-bottom: 12px;
    color: #fff;
    letter-spacing: 1px;
  }

  .server-desc {
    font-size: 0.9rem;
    color: var(--color-text-muted);
    margin-bottom: 20px;
    line-height: 1.6;
  }

  .server-meta-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 15px;
    background: rgba(0, 0, 0, 0.4);
    padding: 15px;
    border-radius: 4px;
    border: 1px solid #1c1638;
    margin-bottom: 25px;
  }

  .meta-item, .meta-item-full {
    display: flex;
    flex-direction: column;
    gap: 5px;
  }

  .meta-item-full {
    grid-column: span 2;
    border-top: 1px dashed #231c3c;
    padding-top: 12px;
    margin-top: 5px;
  }

  .server-meta-grid .label {
    font-size: 0.75rem;
    color: var(--color-text-muted);
  }

  .server-meta-grid .value {
    font-size: 1.2rem;
    color: #fff;
  }

  .highlight-ip {
    color: var(--neon-cyan) !important;
    text-shadow: 0 0 5px var(--neon-cyan-glow);
    display: inline-flex;
    align-items: center;
    gap: 10px;
    width: fit-content;
  }

  .clickable-ip {
    cursor: pointer;
    transition: transform 0.1s ease;
  }

  .clickable-ip:hover {
    transform: scale(1.02);
  }

  .copy-hint {
    font-size: 0.55rem;
    color: var(--color-text-muted);
    border: 1px solid var(--color-text-muted);
    padding: 2px 5px;
    margin-left: 5px;
    transition: all 0.15s ease;
  }

  .clickable-ip:hover .copy-hint {
    color: var(--neon-green);
    border-color: var(--neon-green);
    box-shadow: 0 0 5px var(--neon-green-glow);
  }

  /* Features */
  .server-key-features {
    margin-bottom: 25px;
  }

  .features-title {
    font-size: 0.7rem;
    color: #fff;
    margin-bottom: 12px;
  }

  .features-list {
    list-style: none;
    display: grid;
    grid-template-columns: 1fr 1fr;
    gap: 10px;
  }

  .feature-item {
    font-size: 0.85rem;
    color: var(--color-text);
  }

  /* Actions */
  .server-actions {
    display: flex;
    gap: 20px;
  }

  .server-actions .pixel-button {
    flex: 1;
    font-size: 0.75rem;
  }

  /* Toast Notification */
  .clipboard-toast {
    position: absolute;
    bottom: 20px;
    left: 50%;
    transform: translateX(-50%) translateY(50px);
    background: #0f0a1c;
    color: var(--neon-green);
    border: 2px solid var(--neon-green);
    box-shadow: 0 0 15px var(--neon-green-glow);
    padding: 12px 24px;
    font-size: 0.65rem;
    opacity: 0;
    transition: all 0.3s cubic-bezier(0.175, 0.885, 0.32, 1.275);
    pointer-events: none;
    z-index: 100;
  }

  .clipboard-toast.show {
    opacity: 1;
    transform: translateX(-50%) translateY(0);
  }

  @media (max-width: 576px) {
    .server-meta-grid {
      grid-template-columns: 1fr;
    }
    .meta-item-full {
      grid-column: span 1;
    }
    .features-list {
      grid-template-columns: 1fr;
    }
    .server-actions {
      flex-direction: column;
      gap: 15px;
    }
  }
</style>
